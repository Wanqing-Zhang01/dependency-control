{
  "name": "dependency-control-coach-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy between the Gantt dependency tool frontend and the Gemini API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
